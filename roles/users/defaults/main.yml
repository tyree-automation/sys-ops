---
# Users managed for the host's service life. Created here, removed by the
# decommission role.
#   managed_users:
#     - name: ops
#       sudo: true            # passwordless sudo via /etc/sudoers.d/
#       shell: /bin/bash      # optional, defaults to /bin/bash
#       ssh_keys:             # public keys for authorized_keys
#         - "ssh-ed25519 AAAA... ops@laptop"
managed_users: []
